/**
 * Interactive Transit Map TypeScript interfaces and types
 * DEPRECATED: This file has been split into smaller, focused modules
 * 
 * @deprecated Use imports from src/types/map/ instead:
 * - MapState, MapMode, MapPerformanceConfig → from 'src/types/map/mapState'
 * - MapColorScheme, VehicleColorStrategy → from 'src/types/map/mapColors' 
 * - Layer props interfaces → from 'src/types/map/mapLayers'
 * - All types → from 'src/types/map' (index file)
 */

// Re-export all types from the new modular structure for backward compatibility
export * from './map';